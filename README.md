# Chat API -  Простой чат-сервер

## 💬 Описание
**RESTful API** для чата с поддержкой **Docker Compose**. Хранит сообщения, пользователей и сессии. Готов к продакшену за 1 команду!

## 🚀 Быстрый старт

```bash
# Собрать и запустить (PostgreSQL + API)
make build

# Открыть Swagger docs
http://localhost:8080/swagger/index.html
```

## 📋 Makefile команды

| Команда      | Описание                  | Результат                  |
| ------------ | ------------------------- | -------------------------- |
| `make build` | **Сборка**                | PostgreSQL + API запущены  |
| `make run`   | **Запуск** контейнеров    | Только запуск (без сборки) |
| `make stop`  | **Остановка** контейнеров | Полная очистка             |
| `make test`  | **Unit-тесты** API        | Тесты handlers             |

## 🏗️ Docker Compose сервисы

```
🐳 chat_api          # Go REST API (Gin)
🐘 postgres_db      # PostgreSQL 15
📊 pgadmin          # Веб-интерфейс БД
🌐 nginx            # Reverse proxy + Swagger
```

## 🔧 Технический стек

```
🐳 Docker Compose • PostgreSQL 15
🛠️  Go 1.22 • GORM
🌐 Nginx
🧪 Go Test + Docker
📊 RESTful JSON API
```

## 🚀 Полный workflow

```bash
# 1. Сборка
make build

# 1. Запуск
make run

# 3. Тесты
make test

# 4. Остановка
make stop
```